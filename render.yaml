services:
  - type: web
    name: ggpl-sync
    runtime: node
    buildCommand: npm install
    startCommand: node sync-server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
